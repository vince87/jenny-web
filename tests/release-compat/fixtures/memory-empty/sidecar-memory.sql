-- Phase 12B / A.B.6 release-compat fixture: bare empty database.
-- PRAGMA user_version=0 is the implicit default; declared explicitly so
-- migration tests can rely on the script being self-contained.
PRAGMA user_version=0;
