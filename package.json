{
  "name": "jenny-web-project",
  "version": "0.7.0",
  "private": true,
  "license": "MIT",
  "description": "Local browser workspace derived from Jenny; no Electron runtime",
  "scripts": {
    "start": "node web/server.cjs",
    "test": "node --test web/test/*.test.cjs",
    "format": "prettier --write web services package.json",
    "format:check": "prettier --check web services package.json"
  },
  "devDependencies": {
    "prettier": "3.6.2"
  },
  "engines": {
    "node": ">=24"
  }
}
